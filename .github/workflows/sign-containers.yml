name: Sign Containers

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read
  packages: write
  id-token: write

env:
  REGISTRY: ghcr.io

jobs:
  build-and-sign:
    name: Build and Sign Containers
    runs-on: ubuntu-latest
    timeout-minutes: 30

    strategy:
      matrix:
        component:
          - name: api
            dockerfile: apps/api/Dockerfile
          - name: web
            dockerfile: apps/web/Dockerfile

    steps:
      - uses: actions/checkout@v5

      - name: Normalize repository name
        id: repo
        shell: bash
        run: |
          repo="${GITHUB_REPOSITORY,,}"
          echo "value=${repo%-}" >> "$GITHUB_OUTPUT"

      - name: Login to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.component.dockerfile }}
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ steps.repo.outputs.value }}/${{ matrix.component.name }}:${{ github.ref_name }}
            ${{ env.REGISTRY }}/${{ steps.repo.outputs.value }}/${{ matrix.component.name }}:sha-${{ github.sha }}
          provenance: false

      - name: Install Cosign
        uses: sigstore/cosign-installer@v3

      - name: Sign image keylessly
        env:
          IMAGE: ${{ env.REGISTRY }}/${{ steps.repo.outputs.value }}/${{ matrix.component.name }}
          DIGEST: ${{ steps.build.outputs.digest }}
        run: cosign sign --yes "${IMAGE}@${DIGEST}"

      - name: Verify signature
        env:
          IMAGE: ${{ env.REGISTRY }}/${{ steps.repo.outputs.value }}/${{ matrix.component.name }}
          DIGEST: ${{ steps.build.outputs.digest }}
        run: |
          cosign verify \
            --certificate-identity-regexp "^https://github.com/${GITHUB_REPOSITORY}/.github/workflows/" \
            --certificate-oidc-issuer "https://token.actions.githubusercontent.com" \
            "${IMAGE}@${DIGEST}"
